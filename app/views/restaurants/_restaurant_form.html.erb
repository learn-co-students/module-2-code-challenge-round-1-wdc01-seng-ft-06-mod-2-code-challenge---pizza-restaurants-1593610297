<%= form_for @restaurant do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :address %>
<%= f.text_field :address %>
<%= f.label :pizza_ids, "Pizzas:" %>
<%= f.collection_check_boxes :pizza_ids, Pizza.all, :id, :name %>
<%= f.submit %>
<% end  %>